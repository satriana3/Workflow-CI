name: student_performance_project

env_vars:
  MLFLOW_ENABLE_SYSTEM_MANAGED_RUNS: "false"

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "data/StudentsPerformance_preprocessing.csv"}
    command: "python modelling.py --data_path {data_path}"
